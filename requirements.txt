# RunPod Serverless worker deps
runpod==1.7.7
requests==2.32.3

# Audio I/O
soundfile==0.12.1
librosa==0.10.2
pydub==0.25.1

# VAD (silero-vad via torch.hub is lighter; fallback to the package)
silero-vad==5.1.2

# Denoise
deepfilternet==0.5.6

# Transcription
faster-whisper==1.0.3
ctranslate2==4.4.0

# Diarization — 3.4.x handles torch.load weights_only=True (new default in torch 2.6+)
pyannote.audio==3.4.0

# Pin huggingface-hub to <1.0 — pyannote still uses pre-1.0 'use_auth_token' kwarg
huggingface-hub<1.0,>=0.25

# Speaker embedding / ID
speechbrain==1.0.2

# Utilities
numpy==1.26.4
scipy==1.13.1
